{
  "name": "gstore",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "api-dev": "pnpm --filter @gstore/api dev",
    "api-build": "pnpm --filter @gstore/api build",
    "central-dev": "pnpm --filter @gstore/central dev",
    "central-build": "pnpm --filter @gstore/central build",
    "version": "changeset version",
    "publish": "changeset publish",
    "change": "changeset"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "pnpm": ">=9"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "@changesets/cli": "^2.27.9"
  }
}
